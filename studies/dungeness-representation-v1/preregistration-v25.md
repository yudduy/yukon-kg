# Version 25 Qualification Preregistration

Version 25 supersedes version 24 before any eligible history or R0/R1/R2 treatment. The v24 Splay/GPT-A campaign produced four valid evaluations, two candidate hashes, and two scores across five fresh sessions for $4.156310, but the frozen renderer v2 failed closed: R2 required 32,022 deterministic tokens, above the 32,000-token limit. No v24 event, candidate, score, or view is admitted. The selected 67,008 score equalled the frozen Splay reference and is retained only as diagnostic evidence that this data-file task is likely to saturate the headroom gate.

Renderer v3 changes only the static explanatory sentence in `index.md`; canonical atoms and every R0/R1/R2 index data field remain unchanged. On the excluded v24 manifest, model-free compilation now yields equal atom bytes and total sizes of 31,287, 31,718, and 31,985 tokens. All renderer tests must pass before paid work, and every v25 history is fresh.

Splay is replaced before treatment by the native ECDSA challenge. This choice is based on pre-existing task evidence, not representation results: the sealed 54-event cohort fits the context budget, the verifier is exact and deterministic, the known reference is far from the checkpoint, and the prior single-session seed improved but lacked event density. Dungeness now validates native starting references against their retained verifier tree; external references retain the stricter upstream bundle-lock check.

The six frozen blocks are two independent GPT-5.6 Sol histories each for ECDSA, Toy ISA, and VLIW. Campaign and session caps, convergence thresholds, treatment definitions, randomization, primary outcome, $500 cumulative ceiling, and non-automatic confirmation boundary are unchanged. At least four of the first five campaigns must pass; otherwise the precursor conjecture is killed before treatment. Native ECDSA runs use the already-qualified rootful `dungeness-eval` Colima profile. AutoLab tasks: no declared license as of commit `7aff5fe71dfbe152fb0b8e8ac8087210b4bc27d5`; used for internal evaluation only, not redistributed.
