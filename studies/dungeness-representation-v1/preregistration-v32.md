# Dungeness Representation Pilot v32 Preregistration

## Status

This document is frozen before any v32 model call. v31 is excluded as a
pre-treatment apparatus qualification because immutable candidate permissions
leaked into the disposable agent workspace. No v31 treatment chain ran and the
failure spent $0 on models.

## Fixed scientific design

v32 retains the six qualified, equal-evidence histories and the three frozen
renderers from v31. It changes only the experiment identity, randomization seed,
and repaired workspace permission boundary. The candidate store remains
0444/0555; only its verified disposable copy becomes owner-writable. Research
views remain read-only.

The 18 chains are:

```text
3 tasks x 2 histories x 3 representations x GPT-5.6 Sol
```

Each chain has three fresh sessions and the unchanged aggregate caps of $9,
600,000 model tokens, 90 minutes, and 12 development evaluations. The first
model increment remains capped at $162. Total study model spend remains capped
at $500, with $25.227542 committed before v32 treatment.

## Mandatory release gates

- all Dungeness, gateway, and Yukon tests pass;
- the six histories compile deterministically with exact atom parity;
- all candidate snapshots resolve and retain their recorded hashes;
- native ECDSA and external AutoLab nop smokes reproduce starting metrics;
- candidate workspaces are editable while stores remain immutable;
- research views are mounted read-only;
- all six treatment permutations occur exactly once;
- opaque run specs contain no treatment or block labels;
- the live model catalog still resolves the frozen GPT route and pricing;
- no active spend reservation exists.

If any gate fails, v32 stops before treatment and receives a new version after
repair. Treatment outcomes cannot be used to tune renderers or exclusions.

## Known external-task risk

AutoLab tasks: no declared license as of commit
7aff5fe71dfbe152fb0b8e8ac8087210b4bc27d5; used for internal evaluation only,
not redistributed.
