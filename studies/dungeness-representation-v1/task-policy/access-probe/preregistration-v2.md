# Atlas Access Probe v2 Preregistration

## Repair boundary

V1 is inadmissible. Its first pointer-control session retrieved the target but used an unspecified decision label, and four treatment sessions showed the same output-contract ambiguity. The run then stopped on an exhausted transport retry after `$0.47631572` total spend. V2 changes only the answer-contract disclosure and administrative retry count. It uses six cases never contacted in v1; no v1 response contributes to a representation contrast.

## Frozen apparatus

- Freeze: [`v2-freeze.json`](v2-freeze.json)
- Freeze byte SHA-256: `da3c7b26deafe922aa0e585fa829dbf10df17ecaec2170e64c864f03df4d6562`
- Source release, manifest, reviewed fixture, corpora, query API, budgets, models, randomization, scoring, and decision gates remain those in [v1](preregistration-v1.md).
- Fresh cases: `confirm-positive-02`, `-03`, `-09`, `-11`, `-17`, and `-18`.
- The cases span low and ordinary lexical overlap, focused and bundled evidence, and representative and nonrepresentative witnesses. All pass all-arm mechanical reachability.
- Core-arm source atoms remain byte-identical at `ccc97aa74ae837665060bd8ba2c58160e8a7869771c5b7f3421c97cd036936a6`.

## Single scientific repair

The proposal now includes a literal valid JSON example, every allowed `classification` and `decision` enum, the exact `sourceRefs` field name, and an instruction to return one best grounded match without extra fields. The same bytes are used in raw, flat, and flat-plus-brief. The pointer arm alone adds its declared locator.

Administrative transport retries increase from two to four after the original attempt. Every failed attempt, response ID, and incurred cost remains recorded. This affects missingness handling, not the treatment information.

## Frozen decision

V2 is interpretable only if pointer accuracy is at least 5/6 for each model, at least two cases distinguish core arms for each model, and no provenance is fabricated. The flat mechanism passes only if flat beats raw on at least two cases, loses on none, and satisfies those common gates for **each** model.

- If pointer sensitivity fails: classify v2 as an assay failure.
- If core arms share a floor or ceiling: classify v2 as uninformative.
- If flat fails its paired gate: retain raw chronology for exact lookup.
- If flat passes: classify joined rows as promising specifically for call-bound exact retrieval and freeze a larger study before any further paid execution.
- Flat-plus-brief is descriptive in this exact-lookup task; aggregation requires a new known-answer task.

No outcome can trigger Dungeness chains or a confirmatory study automatically. V2 has a `$20` direct cap; the study-wide `$500` ceiling remains binding.
