{"excerpts":["-    set_default_env(\"DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS\", \"18\");\n+    set_default_env(\"DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS\", \"19\");","+    set_default_env(\"DIALOG_TAIL_NONCE\", \"5012453485\");","+        if nocin {\n+            if stream_suffix >= 2 {\n+                let prefix_len = body_len - stream_suffix;\n+                let c = borrowed_carries.expect(\"nocin requires borrowed carries\");\n+                let (carries, rest) = c.split_at(prefix_len);\n+                let (gated, rest) = rest.split_at(prefix_len);\n+                let scratch = rest[0];\n+                b.set_phase(\"dialog_gcd_raw_tobitvector_materialized_add_load\");\n+                for j in 0..prefix_len {\n+                    b.ccx(ctrl, addend[body_start + j], gated[j]);\n+                }\n+                b.cx(ctrl, acc[0]);\n+                b.set_phase(\"dialog_gcd_raw_tobitvector_materialized_add_body\");\n+                cuccaro_add_fast_prefix_ctrl_suffix_no_cin(\n+                    b,\n+                    gated,\n+                    &addend[body_start + prefix_len..body_w],\n+                    &acc[body_start..body_w],\n+                    ctrl,\n+                    carries,\n+                    scratch,\n+                );\n+                b.set_phase(\"dialog_gcd_raw_tobitvector_materialized_add_clear\");\n+                for j in 0..prefix_len {\n+                    let m = b.alloc_bit();\n+                    b.hmr(gated[j], m);\n+                    b.cz_if(ctrl, addend[body_start + j], m);\n+                }\n+                return;","-    set_default_env(\"DIALOG_TAIL_NONCE\", \"1200006462\");\n+    set_default_env(\"DIALOG_TAIL_NONCE\", \"7000001200\");","+        // 4) cswap(swap_flag, u, v). For i>=1, bit 0 is skipped: the odd-u\n+        //    invariant gives u[0]=1, and subtracted=v[0], so when swap_flag=1\n+        //    both are 1 (no-op swap); when swap_flag=0 no swap anyway.\n+        let j_start = if i > 0 { 1 } else { 0 };\n+        for j in j_start..current_n {\n+            circ.cswap(swap_flag, u[j], v[j]);\n+        }","+    circ.cx(*ctrl, y[0]); // bit-0 sum (x[0] == 1 under the odd-u invariant)\n     if x.len() > 1 {\n         let yr: Vec<&QubitId> = y[1..].iter().collect();\n         let xr: Vec<&QubitId> = x[1..].iter().collect();\n         super::gidney::controlled_hybrid_add_capped_branch(circ, ctrl, &yr, &xr, k, super::PAD, branch);","+    // Stage 1: prod = lambda^2 (integer, 2n bits).\n+    let mut prod: Vec<QubitId> = Vec::with_capacity(2 * n);\n+    symmetric_square_into_prod(circ, lambda, &mut prod);\n+\n+    // Stage 2: output -= (lo + f*hi) mod q, operating on prod's own halves.\n+    //   lo = prod[0..n]                                  (n-bit, lo can be >= q)\n+    //   hi = prod[n..2n]                                 (n-bit, hi < q)\n+    // mod_double needs a 257-bit operand whose top bit is |0>; one inserted pad\n+    // above hi supplies that overflow slot (restored, removed at the end).\n+    {\n+\n+        // --- lo term: output -= lo mod q ---\n+        // lo = prod[0..n] is a full integer < 2^256 (not pre-reduced), but\n+        // mod_sub subtracts mod q, which is the value we want:\n+        // lambda^2 mod q == (lo + f*hi) mod q == (lo mod q + ...).\n+        // UNCONTROLLED: no |1>-gated register-sub CCX.\n+        mod_sub(circ, &prod[0..n], output_reg);\n+\n+        let pad_hi = circ.alloc_qubit();\n+        let mut hi_ext: Vec<QubitId> = prod[n..2 * n].to_vec();\n+        hi_ext.push(pad_hi); // index 2n = overflow slot, |0>\n+\n+        // --- hi terms: output -= (hi*2^j mod q) for each f-bit j ---\n+        let mut shifted = 0usize;\n+        for &j in &F_BITS {\n+            while shifted < j {\n+                mod_double(circ, &hi_ext); // hi *= 2 mod q (257-bit, pad restored)\n+                shifted += 1;\n+            }\n+            mod_sub(circ, &hi_ext[0..n], output_reg);\n+        }\n+        // restore hi to its raw value (matched-reverse the doublings). mod_double\n+        // is built from a swap chain (self-inverse) + a +f window (NOT self-\n+        // inverse), so a literal re-run does NOT undo it -- use the value-inverse\n+        // mod_double_reverse (one halving per forward doubling).\n+        while shifted > 0 {\n+            mod_double_reverse(circ, &hi_ext);\n+            shifted -= 1;\n+        }"],"schema":"yukon.atlas","schemaVersion":3,"shard":"91","submissions":[{"evidence":[{"changeId":"918851de-37b5-4f74-b685-3fae4c8bdd83::m1","excerptIndex":0},{"changeId":"918851de-37b5-4f74-b685-3fae4c8bdd83::m2","excerptIndex":1},{"changeId":"918851de-37b5-4f74-b685-3fae4c8bdd83::m3","excerptIndex":2}],"note":null,"submissionId":"918851de-37b5-4f74-b685-3fae4c8bdd83"},{"evidence":[{"changeId":"918ec6db-2d72-4a5a-ad82-a243c59c5ad1::m1","excerptIndex":3}],"note":"Same accepted q1164 trailmix-ludicrous Karatsuba-square circuit; only the baked\nDIALOG_TAIL_NONCE is re-hunted to 7000001200 (env-less). A deeper-tail draw lowers the","submissionId":"918ec6db-2d72-4a5a-ad82-a243c59c5ad1"},{"evidence":[{"changeId":"91c5643b-b522-4f2e-a32e-ec0e937c935f::m1","excerptIndex":4},{"changeId":"91c5643b-b522-4f2e-a32e-ec0e937c935f::m2","excerptIndex":5},{"changeId":"91c5643b-b522-4f2e-a32e-ec0e937c935f::m3","excerptIndex":6}],"note":"Skip bit-0 of GCD cswap for iterations i>0 (odd-u invariant: u[0]=v[0]=1 when swap fires, making the cswap a no-op). Saves 1,023 avg Toffoli across 4 GCD passes. Nonce 13003.","submissionId":"91c5643b-b522-4f2e-a32e-ec0e937c935f"}],"view":"submission-detail"}
