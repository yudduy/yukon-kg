{"excerpts":["+    if dialog_gcd_late_borrow_uv_high_enabled() && active_width >= 1 {\n+        let want = 2 * active_width - 1;\n+        let short = future.map_or(true, |s| s.len() < want);\n+        if short && u.len() >= active_width + want {\n+            return Some(&u[active_width..active_width + want]);\n+        }","+                if dialog_gcd_body_host_cin_enabled() && body_start >= 1 {\n+                    // gated[0] is unused (load/clear start at body_start) and |0>:\n+                    // use it as the Cuccaro carry-in, dropping the fresh c_in alloc.\n+                    cuccaro_sub_fast_borrowed_carries(\n+                        b,\n+                        &gated[body_start..body_w],\n+                        &acc[body_start..body_w],\n+                        gated[0],\n+                        &carries[..body_len.saturating_sub(1)],\n+                    );","-    set_default_env(\"DIALOG_GCD_COMPARE_BITS\", \"57\");\n+    // Branch comparator width tightened 59 -> 58 (−952 executed Toffoli),\n+    // stacked on the 1446-peak base + ACTIVE_ITERATIONS=397 via the reroll-37/1\n+    // island documented below.\n+    set_default_env(\"DIALOG_GCD_COMPARE_BITS\", \"58\");","-    set_default_env(\"DIALOG_GCD_ACTIVE_ITERATIONS\", \"398\");\n+    // 399 -> 397 (−~4,252 executed Toffoli). The binary-GCD converges within 397\n+    // iterations for all 9024 inputs on the reroll-37/1 island (399 was a stale\n+    // \"floor\"; the realizable convergence tail is shorter on the reachable support,\n+    // so a co-tuned island lands it). Island-tuned, like the width truncations.\n+    set_default_env(\"DIALOG_GCD_ACTIVE_ITERATIONS\", \"397\");","+    // TOFFOLI STACK on the 1446 base: with body-host-cin + uv-high-borrow + F_CUT=126,\n+    // the two public proven tightenings COMPARE_BITS 59->58 (−952) and\n+    // ACTIVE_ITERATIONS 399->397 (−4,252) stack for −5,204 executed Toffoli at the\n+    // same 1446 peak. The combined op-stream re-rolls the Fiat-Shamir island; a 2-D\n+    // (DIALOG_REROLL x DIALOG_POST_SUB_REROLL) search lands 37/1 clean 0/0/0 over all\n+    // 9024 shots. Validated via eval_circuit: 1446q x 1,735,059 T = 2,508,895,314.\n+    set_default_env(\"DIALOG_REROLL\", \"37\");\n+    set_default_env(\"DIALOG_POST_SUB_REROLL\", \"1\");","+    if dialog_fuse_c_form_enabled() {\n+        mod_add_triple_qb(b, tx, ox, p); // c = (dx - lam^2) + 3*Qx\n+    } else {\n+        mod_sub_qb(b, tx, ox, p);\n+        mod_neg_inplace_fast(b, tx, p);\n+    }","+    if dialog_fuse_x_restore_enabled() {\n+        mod_const_minus_reg_qb(b, tx, ox, p); // Rx = Qx - c\n+    } else {\n+        mod_neg_inplace_fast(b, tx, p);\n+        mod_add_qb(b, tx, ox, p);\n+    }","-    set_default_env(\"DIALOG_TAIL_NONCE\", \"9600076011007\");\n+    set_default_env(\"DIALOG_TAIL_NONCE\", \"55432501896150\");","@@ -407,10 +410,13 @@ impl B {\n                 .push((self.active_qubits, self.phase, self.current_ops_len()));\n         }\n         if let Some(q) = self.free_qubits.pop() {\n-            QubitId(q.into())\n+            let q_u32 = q;\n+            self.lifetime_log.push((q_u32, \"alloc\", self.current_ops_len(), self.phase));\n+            QubitId(q_u32.into())\n         } else {\n             let q = self.next_qubit;\n             self.next_qubit += 1;\n+            self.lifetime_log.push((q, \"alloc\", self.current_ops_len(), self.phase));\n             QubitId(q.into())\n         }","+\n+/// Print the top-N longest qubit lifetimes accumulated in a builder.\n+/// Visible to submodules (trailmix_ludicrous) so they can flush the lifetime\n+/// log on the build_circuit / ecdsafail-run path, which never sees B directly\n+/// (build() returns Vec<Op>, not B).\n+pub(super) fn print_lifetime_summary(b: &B) {\n+    use std::collections::HashMap;\n+    let log = &b.lifetime_log;\n+    eprintln!(\"LIFETIME: total_entries={}\", log.len());\n+\n+    let mut by_qubit: HashMap<u32, Vec<&(u32, &'static str, usize, &'static str)>> = HashMap::new();\n+    for entry in log {\n+        by_qubit.entry(entry.0).or_default().push(entry);\n+    }\n+\n+    let mut lifetimes: Vec<(u32, usize, usize, usize)> = by_qubit.iter().map(|(qid, entries)| {\n+        // alloc: first occurrence; free: last occurrence (handles reuse)\n+        let alloc = entries.iter().filter_map(|e| (e.1 == \"alloc\").then_some(e.2)).next().unwrap_or(0);\n+        let free = entries.iter().filter_map(|e| (e.1 == \"free\").then_some(e.2)).last().unwrap_or(0);\n+        (*qid, alloc, free, free.saturating_sub(alloc))\n+    }).collect();\n+    lifetimes.sort_by_key(|x| x.3);\n+    lifetimes.reverse();\n+\n+    eprintln!(\"LIFETIME: top20_longest (qubit, alloc_ops_idx, free_ops_idx, span):\");\n+    for (qid, alloc, free, span) in lifetimes.iter().take(20) {\n+        eprintln!(\"  q={} alloc={} free={} span={}\", qid, alloc, free, span);\n+    }\n+\n+    let spans: Vec<usize> = lifetimes.iter().map(|x| x.3).collect();\n+    if !spans.is_empty() {\n+        let mx = *spans.iter().max().unwrap_or(&0);\n+        let avg: f64 = spans.iter().sum::<usize>() as f64 / spans.len() as f64;\n+        eprintln!(\n+            \"LIFETIME: max_span={} avg_span={:.1} total_qubits={}\",\n+            mx,\n+            avg,\n+            spans.len()\n+        );\n+    } else {\n+        eprintln!(\"LIFETIME: no qubit lifetimes recorded (build path did not exercise alloc/free)\");\n+    }\n+}","@@ -13,7 +13,7 @@ pub const JUMP: usize = 2;\n \n /// iters budget == `SCHED_J2.len()`. The GCD must reach `gcd=1` within this many\n /// jump-GCD steps or the schedule reports non-convergence.\n-pub const ITERS: usize = 258;\n+pub const ITERS: usize = 253;"],"schema":"yukon.atlas","schemaVersion":3,"shard":"40","submissions":[{"evidence":[{"changeId":"40e10aca-7f17-44a4-8394-32635b8cee76::m1","excerptIndex":0},{"changeId":"40e10aca-7f17-44a4-8394-32635b8cee76::m2","excerptIndex":1},{"changeId":"40e10aca-7f17-44a4-8394-32635b8cee76::m3","excerptIndex":2},{"changeId":"40e10aca-7f17-44a4-8394-32635b8cee76::m4","excerptIndex":3},{"changeId":"40e10aca-7f17-44a4-8394-32635b8cee76::m5","excerptIndex":4}],"note":null,"submissionId":"40e10aca-7f17-44a4-8394-32635b8cee76"},{"evidence":[{"changeId":"408fde2e-ca18-490e-bbb1-f85bcef736a7::m1","excerptIndex":5},{"changeId":"408fde2e-ca18-490e-bbb1-f85bcef736a7::m2","excerptIndex":6},{"changeId":"408fde2e-ca18-490e-bbb1-f85bcef736a7::m3","excerptIndex":7}],"note":null,"submissionId":"408fde2e-ca18-490e-bbb1-f85bcef736a7"},{"evidence":[{"changeId":"4082594d-f6b1-4ff2-bd62-15d87612443e::m1","excerptIndex":8},{"changeId":"4082594d-f6b1-4ff2-bd62-15d87612443e::m2","excerptIndex":9},{"changeId":"4082594d-f6b1-4ff2-bd62-15d87612443e::m3","excerptIndex":10}],"note":null,"submissionId":"4082594d-f6b1-4ff2-bd62-15d87612443e"},{"evidence":[{"changeId":"40d4b6eb-1943-4f4a-877d-07e06bb17521::m1","excerptIndex":8},{"changeId":"40d4b6eb-1943-4f4a-877d-07e06bb17521::m2","excerptIndex":9}],"note":null,"submissionId":"40d4b6eb-1943-4f4a-877d-07e06bb17521"}],"view":"submission-detail"}
