{"excerpts":["+/// UMA specialized for a `w` operand bit that is provably |0> at entry (M023).\n+///\n+/// The plain `uma(x, y, w) = ccx(x,y,w); cx(w,x); cx(x,y)` uncomputes a target that,\n+/// for a zero-entry operand, holds exactly `AND(x, y)` (the carry the matching `maj`\n+/// ANDed into a fresh |0>). Its Toffoli can therefore be replaced with the shipped\n+/// measurement-based AND-uncompute idiom (`hmr` + `cz_if`, identical to the one used in\n+/// `cuccaro_add_fast` above), removing one CCX per site, bit-exactly. `cx(w,x)` is a\n+/// no-op because `w` is |0> after the measured clear, so only the trailing `cx(x,y)`\n+/// survives.\n+pub(crate) fn uma_from_zero(b: &mut B, x: QubitId, y: QubitId, w: QubitId) {\n+    let m = b.alloc_bit();\n+    b.hmr(w, m);\n+    b.cz_if(x, y, m);\n+    b.cx(x, y);\n+}","+    let mut ops = trailmix_ludicrous::constprop::ccx_final_cancel(ops);\n+    // E275: re-run single_ccx_fanout to fixpoint over the post-cancel stream (-13 CCX, bit-exact).\n+    if std::env::var(\"SINGLE_CCX_FANOUT_SECOND_PASS\").ok().as_deref() != Some(\"0\") {\n+        loop {\n+            match single_ccx_fanout::rewrite_first_target_fanout(ops.clone(), 96) {\n+                Ok((rewritten, _w)) => { ops = rewritten; }\n+                Err(_e) => break,\n+            }\n+        }\n+    }","-        12 | 25 => (1..=127).contains(&bit),\n+        12 | 25 => (0..=127).contains(&bit),\n         37 => bit <= 135,\n-        19 => (1..=127).contains(&bit),\n+        19 => (0..=127).contains(&bit),\n         20 | 26 => bit >= 148,","+        // E208: ops 18..=26 form one 5-control Toffoli w12 ^= AND(w8,w9,w10,w13,w14),\n+        // 9 Toffoli-class gates (2 clean anc). Replace with mcx_clean_k: 7 gates, 3 clean anc.\n+        // 5-ctrl Toffoli is an involution => same call for forward and reverse.\n+        if i == 18 {\n+            super::mcx::mcx_clean_k(\n+                circ,\n+                &[\n+                    w[(8 - off) as usize],\n+                    w[(9 - off) as usize],\n+                    w[(10 - off) as usize],\n+                    w[(13 - off) as usize],\n+                    w[(14 - off) as usize],\n+                ],\n+                w[(12 - off) as usize],\n+            );\n+            continue;\n+        }\n+        if (19..=26).contains(&i) {\n+            continue;\n+        }"," fn gcd_forward_cswap_has_structurally_dead_gate(step: usize, bit: usize) -> bool {\n+    // W1155: forward structurally-dead-cswap predicate := reverse predicate (bit-exact,\n+    // the forward pass shares the reverse pass's provably-dead structure).\n+    if std::env::var_os(\"W1155_FWD_EQ_REV\").is_some() {\n+        return gcd_reverse_cswap_has_structurally_dead_gate(step, bit);\n+    }","+        match plan[k] {\n+            Step::X0(t) => circ.x(t),\n+            Step::X1(c, t) => circ.cx(c, t),\n+            Step::X2(x, y, t) => circ.ccx(x, y, t),\n+            Step::Xanc(t) => circ.x(t),\n+            Step::Uncmp(x, y, t) => {\n+                if vent_pure[k] {\n+                    mbu_clear_and(circ, &t, &x, &y);\n+                } else if vent_xc[k] {\n+                    circ.x(t);\n+                    mbu_clear_and(circ, &t, &x, &y);\n+                } else {\n+                    circ.ccx(x, y, t);\n+                }","+CLEAN_AUX_SIZE = 3\n+DIRTY_PASSENGER_SIZE = 10","+def _upper_zero_map_midpoint_three_dirty(\n+    qc: QuantumCircuit,\n+    *,\n+    ctrl: Qubit,\n+    boundary_B: Sequence[Qubit],\n+    bits: Sequence[Qubit],\n+    dirty_map: Sequence[Qubit],\n+    dirty: Sequence[Qubit],\n+    scratch: Sequence[Qubit],\n+) -> None:\n+    \"\"\"Exact three-clean upper-zero map used with a stable copied carry.\"\"\"\n+    if len(bits) != 259 or len(dirty_map) != 259 or len(scratch) < 3:\n+        raise ValueError(\"three-clean midpoint map workspace mismatch\")","+    def copy_selected_carry() -> None:\n+        def leaf(encoded: int, controls: Sequence[Qubit]) -> None:\n+            _toggle_raw_controls_dirty(\n+                qc, list(controls) + [Work1[encoded + 1]], acc, Dirty[4:],\n+            )\n+\n+        unary_iteration_dirty_32raw(\n+            qc, index_reg=l_t, labels=encoded_labels,\n+            ctrl=Ctrl[0], ancillas=Scratch[:3],\n+            leaf_fn=leaf, order=\"inc\",\n+        )\n+\n+    def selected_carry_sign_toggle() -> None:\n+        def leaf(encoded: int, controls: Sequence[Qubit]) -> None:\n+            _toggle_raw_controls_dirty(\n+                qc, list(controls) + [Work1[encoded + 2]],\n+                Sign[0], Dirty[4:],\n+            )\n+\n+        unary_iteration_dirty_32raw(\n+            qc, index_reg=l_t, labels=encoded_labels, ctrl=acc,\n+            ancillas=Scratch[:3], leaf_fn=leaf, order=\"inc\",\n+        )\n+\n+    if encoded_labels:\n+        # The range accumulator has returned to zero on the certified l_t\n+        # support.  Copy the dynamic carry into that fixed clean lane, then use\n+        # a two-map commutator.  The copied carry remains stable while Work1 is\n+        # the dirty upper-zero map, so two maps replace the former four-map\n+        # dirty-passenger sandwich.\n+        copy_selected_carry()\n+        selected_carry_sign_toggle()\n+        _upper_zero_map_midpoint_three_dirty(\n+            qc, ctrl=Ctrl[0], boundary_B=l_s, bits=Work2,\n+            dirty_map=Work1, dirty=Dirty, scratch=Scratch[:3],\n+        )\n+        selected_carry_sign_toggle()\n+        _upper_zero_map_midpoint_three_dirty(\n+            qc, ctrl=Ctrl[0], boundary_B=l_s, bits=Work2,\n+            dirty_map=Work1, dirty=Dirty, scratch=Scratch[:3],\n+        )\n+        copy_selected_carry()"],"schema":"yukon.atlas","schemaVersion":3,"shard":"de","submissions":[{"evidence":[{"changeId":"def915cb-2fa2-4ca7-8a25-cf6aa4eb8c11::m1","excerptIndex":0},{"changeId":"def915cb-2fa2-4ca7-8a25-cf6aa4eb8c11::m2","excerptIndex":1},{"changeId":"def915cb-2fa2-4ca7-8a25-cf6aa4eb8c11::m3","excerptIndex":2},{"changeId":"def915cb-2fa2-4ca7-8a25-cf6aa4eb8c11::m4","excerptIndex":3},{"changeId":"def915cb-2fa2-4ca7-8a25-cf6aa4eb8c11::m5","excerptIndex":4},{"changeId":"def915cb-2fa2-4ca7-8a25-cf6aa4eb8c11::m6","excerptIndex":5}],"note":null,"submissionId":"def915cb-2fa2-4ca7-8a25-cf6aa4eb8c11"},{"evidence":[{"changeId":"de427c61-a798-48ae-8886-232f85c0d41a::m1","excerptIndex":6},{"changeId":"de427c61-a798-48ae-8886-232f85c0d41a::m2","excerptIndex":7},{"changeId":"de427c61-a798-48ae-8886-232f85c0d41a::m3","excerptIndex":8}],"note":null,"submissionId":"de427c61-a798-48ae-8886-232f85c0d41a"}],"view":"submission-detail"}
