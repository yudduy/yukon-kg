{"excerpts":["+    set_default_env(\"DIALOG_GCD_WIDTH_SLOPE_X1000\", \"1016\");","+    set_default_env(\"DIALOG_TAIL_NONCE\", \"19080008907\");","+    // ── 3. free the four e,d-derived controls BEFORE allocating the wide tail ──\n+    //   (reset them to |0> via the free-CX uncompute + a measured AND-clear, then\n+    //   release the SAME qubits; reacquired+re-derived in step 6 so the caller\n+    //   sees them live & correct on return, exactly as the baseline ripple does.)\n+    b.cx(h, n10);\n+    b.cx(d, n10);\n+    b.cx(h, eord);\n+    b.cx(xed, eord);\n+    b.cx(d, xed);\n+    b.cx(e, xed);\n+    b.free(n10);\n+    b.free(eord);\n+    b.free(xed);\n+    let mh = b.alloc_bit();\n+    b.hmr(h, mh);\n+    b.cz_if(e, d, mh);\n+    b.free(h);\n+\n+    // Tail carries are allocated NOW (4 derived controls already freed ⇒ the\n+    // wide-lane high-water carries +4 ancillae instead of +8).\n+    let tail_len = last - hi_delta;\n+    let tail = b.alloc_qubits(tail_len);","+    set_default_env(\"DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS\", \"11\");","+    set_default_env(\"SQUARE_ROW_MAX_SEG\", \"194\");","+    fn new_count_only() -> Self {\n+        let mut b = Self::new();\n+        b.count_only = true;\n+        b.fiat_hash = Self::fiat_hash_from_env();\n+        b\n+    }\n+    fn fiat_hash_from_env() -> Option<Shake256> {\n+        let ops_len = std::env::var(\"POINT_ADD_HASH_OPS_LEN\")\n+            .ok()\n+            .and_then(|s| s.parse::<u64>().ok())?;\n+        let mut hasher = Shake256::default();\n+        hasher.update(b\"quantum_ecc-fiat-shamir-v2\");\n+        hasher.update(&ops_len.to_le_bytes());\n+        Some(hasher)\n+    }\n+    pub(crate) fn update_fiat_hash_op(hasher: &mut Shake256, op: &Op) {\n+        hasher.update(&[op.kind as u8]);\n+        hasher.update(&op.q_control2.0.to_le_bytes());\n+        hasher.update(&op.q_control1.0.to_le_bytes());\n+        hasher.update(&op.q_target.0.to_le_bytes());\n+        hasher.update(&op.c_target.0.to_le_bytes());\n+        hasher.update(&op.c_condition.0.to_le_bytes());\n+        hasher.update(&op.r_target.0.to_le_bytes());\n+    }\n+    pub(crate) fn clone_fiat_hash(&self) -> Option<Shake256> {\n+        self.fiat_hash.clone()\n+    }\n+    fn push_op(&mut self, op: Op) {\n+        self.counted_ops += 1;\n+        self.counted_kind_ops[op.kind as usize] += 1;\n+        self.counted_phase_kind_ops[op.kind as usize] += 1;\n+        if let Some(hasher) = &mut self.fiat_hash {\n+            Self::update_fiat_hash_op(hasher, &op);\n+        }","+pub fn build() -> Vec<Op> {\n+    if std::env::var(\"POINT_ADD_DIALOG_ROUTE\").ok().as_deref() != Some(\"1\") {\n+        return trailmix_port::build_builder().ops;","+pub fn ec_add_inplace_shrunken_pz(\n+    circ: &mut Circuit,\n+    tx: &mut Vec<QReg>,\n+    ty: &mut Vec<QReg>,\n+    ox: &[Cbit],\n+    oy: &[Cbit],\n+) {\n+    use crate::point_add::trailmix_port::inversion::shrunken_pz_state_machine::{\n+        shrunken_pz_divide_cancel, shrunken_pz_divide_forward,\n+    };\n+    assert_eq!(tx.len(), 256, \"tx is a 256-bit value register (P.x -> R.x)\");\n+    assert_eq!(ty.len(), 256, \"ty is a 256-bit value register (P.y -> R.y)\");\n+    assert_eq!(ox.len(), 256);\n+    assert_eq!(oy.len(), 256);\n+\n+    // Pad to 257-bit work registers (high overflow bit |0>) for the in-place mod\n+    // arithmetic; the shrunken-PZ divide needs the 257th sign bit. Both values are\n+    // canonical [0,p) on entry and exit, so the overflow bit is |0> and is freed\n+    // before return -- the public interface is 256-bit in/out.\n+    tx.push(circ.alloc_qreg(\"ec3.tx_ov\"));\n+    ty.push(circ.alloc_qreg(\"ec3.ty_ov\"));\n+\n+    // Phase 1: ty := dy = oy - ty.\n+    circ.set_section(\"ec3.dy_build\");\n+    if low_pressure_creg_qload_enabled() {\n+        mod_sub_from_creg_qload_w(circ, &ty[..], oy);\n+    } else {\n+        mod_sub_from_creg_w(circ, &ty[..], oy);\n+    }\n+\n+    // Phase 2: tx := dx = ox - tx. (Keep 257 -- shrunken_pz divide needs the sign bit.)\n+    circ.set_section(\"ec3.dx_build\");\n+    if low_pressure_creg_qload_enabled() {\n+        mod_sub_from_creg_qload_w(circ, &tx[..], ox);\n+    } else {\n+        mod_sub_from_creg_w(circ, &tx[..], ox);\n+    }\n+\n+    // Phase 3: lambda = dy/dx (dx, dy preserved).\n+    circ.set_section(\"ec3.inv_fwd\");\n+    let dx_inner = std::mem::take(tx);\n+    let dy_vec = std::mem::take(ty);\n+    let (dx_inner, dy_vec, lambda) = shrunken_pz_divide_forward(circ, dx_inner, dy_vec);","+fn trailmix_q_width_step(wq: usize, wa: usize, wb: usize, wca: usize, wcb: usize) -> usize {\n+    let natural = wq.max(1);\n+    let target = std::env::var(\"TRAILMIX_Q_TARGET\")\n+        .ok()\n+        .and_then(|s| s.parse::<usize>().ok());\n+    let Some(target) = target else {\n+        return trailmix_q_width(wq);\n+    };\n+    // q budget is computed from the (possibly capped) A/B and ca/cb widths so the\n+    // working width 2*ab + 2*cacb + q meets `target` consistently with the resizes.\n+    let other = 2 * trailmix_ab_width(wa.max(wb)) + 2 * trailmix_cacb_width(wca.max(wcb));\n+    let budget = target.saturating_sub(other).max(1);\n+    // Still honor a global Q_CAP if both are set (take the tighter bound).\n+    let capped = natural.min(budget);\n+    std::env::var(\"TRAILMIX_Q_CAP\")\n+        .ok()\n+        .and_then(|s| s.parse::<usize>().ok())\n+        .map_or(capped, |cap| capped.min(cap.max(1)))\n+        .max(1)","+fn configure_sub1000_trailmix_route() {\n+    set_default_env(\"TRAILMIX_THIN_SCHEDULE\", \"1\");\n+    set_default_env(\"TRAILMIX_THIN_SEED\", \"278\");\n+    set_default_env(\"TRAILMIX_THIN_CLZ_WINDOW\", \"78\");\n+    set_default_env(\"TRAILMIX_THIN_MARGIN\", \"0\");\n+    set_default_env(\"TRAILMIX_THIN_VALIDATE\", \"500000\");\n+    set_default_env(\"TRAILMIX_COUNTER_W\", \"8\");","+    if let Some(nonce) = std::env::var(\"TRAILMIX_TAIL_NONCE\")\n+        .ok()\n+        .and_then(|s| s.parse::<u64>().ok())\n+    {\n+        circ.set_section(\"trailmix_tail_nonce\");\n+        for i in 0..TRAILMIX_TAIL_NONCE_BITS {\n+            let q = if (nonce >> i) & 1 == 1 {\n+                &out[1]\n+            } else {\n+                &out[0]\n+            };\n+            circ.x(q);\n+            circ.x(q);\n+        }","-    set_default_env(\"DIALOG_TAIL_NONCE\", \"800457469\");\n+    set_default_env(\"DIALOG_TAIL_NONCE\", \"82979\");","+pub(crate) fn run(ops: &[Op]) {\n+    let rounds: u64 = std::env::var(\"TLM_CENSUS_ROUNDS\")\n+        .ok()\n+        .and_then(|s| s.parse().ok())\n+        .unwrap_or(64);\n+    let threads: usize = std::env::var(\"TLM_CENSUS_THREADS\")\n+        .ok()\n+        .and_then(|s| s.parse().ok())\n+        .unwrap_or(std::thread::available_parallelism().map(|n| n.get()).unwrap_or(4));\n+    // Round index seeds the input draw, so a disjoint offset gives a disjoint input set.\n+    // Mine on one range and re-census on another to get a genuine holdout: any candidate\n+    // whose predicate is violated on the held-out set was a sampling artefact, and that\n+    // is the only way to bound the false-positive rate from the inside.","-    (13, 891, 611, 9, 18446744073709551615, 0, 1),\n-    (13, 892, 610, 1147, 18446744073709551615, 0, 1),\n-    (13, 893, 609, 1153, 18446744073709551615, 0, 1),\n-    (13, 126, 608, 607, 18446744073709551615, 0, 2),\n-    (13, 895, 607, 606, 18446744073709551615, 0, 2),\n-    (13, 896, 606, 128, 18446744073709551615, 0, 2),\n-    (13, 896, 606, 128, 18446744073709551615, 1, 2),\n-    (13, 895, 607, 606, 18446744073709551615, 1, 2),\n-    (13, 126, 608, 607, 18446744073709551615, 1, 2),"],"schema":"yukon.atlas","schemaVersion":3,"shard":"15","submissions":[{"evidence":[{"changeId":"15f3edf5-68b7-4b6b-bade-550fb7c930b4::m1","excerptIndex":0},{"changeId":"15f3edf5-68b7-4b6b-bade-550fb7c930b4::m2","excerptIndex":1}],"note":null,"submissionId":"15f3edf5-68b7-4b6b-bade-550fb7c930b4"},{"evidence":[{"changeId":"155ebc56-b6e3-42c1-8246-0556cd8e8e80::m1","excerptIndex":2},{"changeId":"155ebc56-b6e3-42c1-8246-0556cd8e8e80::m2","excerptIndex":3},{"changeId":"155ebc56-b6e3-42c1-8246-0556cd8e8e80::m3","excerptIndex":4}],"note":"- **DIALOG_GCD_FOLD_FREED_TAIL=1** — round84 Solinas-fold \"freed-tail\" lever: recomputes one ccx(e,d,h) per fold call (~+511 T) to free the fold's top carry lane instead of holding it, dropping the fold binder below the global peak. Coherent (no measurement added) ⇒ value-exact; differential selftest FOLD_FREED_TAIL_SELFTEST passes, ancilla-clean.\n\n- **SQUARE_ROW_MAX_SEG=194** (was 199) — tightens the peak-bounded round84 square so its peak lands at exactly 1221 (no waste).\n\n- **DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS=11** (was 10) — co-descends the apply-ripple below 1221 at minimal cost.","submissionId":"155ebc56-b6e3-42c1-8246-0556cd8e8e80"},{"evidence":[{"changeId":"15206f69-e468-45ba-9a23-ede2715782d1::m1","excerptIndex":5},{"changeId":"15206f69-e468-45ba-9a23-ede2715782d1::m2","excerptIndex":6},{"changeId":"15206f69-e468-45ba-9a23-ede2715782d1::m3","excerptIndex":7},{"changeId":"15206f69-e468-45ba-9a23-ede2715782d1::m4","excerptIndex":8},{"changeId":"15206f69-e468-45ba-9a23-ede2715782d1::m5","excerptIndex":9},{"changeId":"15206f69-e468-45ba-9a23-ede2715782d1::m6","excerptIndex":10}],"note":null,"submissionId":"15206f69-e468-45ba-9a23-ede2715782d1"},{"evidence":[{"changeId":"154c6113-55b8-4d33-bf9f-3ea2192fdfcf::m1","excerptIndex":11}],"note":"change is the fixed-cost identity-tail nonce, updated to `82979` after a\nGPU-filtered search over nonce range `[0, 262144)`.","submissionId":"154c6113-55b8-4d33-bf9f-3ea2192fdfcf"},{"evidence":[],"note":null,"submissionId":"15da4169-f589-4b27-b91b-7abc622304d5"},{"evidence":[{"changeId":"15a44a2e-d8be-488f-9f10-7aeec350b0e9::m1","excerptIndex":12},{"changeId":"15a44a2e-d8be-488f-9f10-7aeec350b0e9::m2","excerptIndex":13}],"note":null,"submissionId":"15a44a2e-d8be-488f-9f10-7aeec350b0e9"}],"view":"submission-detail"}
