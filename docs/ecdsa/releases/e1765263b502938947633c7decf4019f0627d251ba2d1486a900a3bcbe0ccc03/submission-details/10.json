{"excerpts":["-    // The tail nonce reseeds the 9024 Fiat-Shamir draws so all land in the\n-    // schedule-supported set: nonce 2686 validates 0/0/0 over all 9024\n-    // shots at 1165q x 1,413,439.226 => 1,413,439 x 1165 = 1,646,656,435.\n+    // The tail nonce reseeds the 9024 Fiat-Shamir draws so all land in the\n+    // schedule-supported set: nonce 22275 validates 0/0/0 over all 9024 shots\n+    // with the lane-0 GCD cswap elision at\n+    // 1164q x 1,412,443.306 => 1,412,443 x 1164 = 1,644,083,652.\n     set_default_env(\"LUD_EXTRA_FOLD_VENTS\", \"2\");\n     set_default_env(\"LUD_EXTRA_FOLD_MIN_G\", \"16\");\n-    set_default_env(\"DIALOG_TAIL_NONCE\", \"100000025120\");\n+    set_default_env(\"DIALOG_TAIL_NONCE\", \"22275\");","@@ -353,7 +353,7 @@ pub fn forward_gcd_jump(circ: &mut B, v: &mut Vec<QubitId>, apply_inv: Option<(&\n             sf\n         };\n         // 4) cswap(swap_flag, u, v).\n-        for j in 0..current_n {\n+        for j in 1..current_n {\n             circ.cswap(swp, u[j], v[j]);\n         }","-    set_default_env(\"DIALOG_TAIL_NONCE\", \"800000341\");\n+    set_default_env(\"DIALOG_TAIL_NONCE\", \"800006524\");","-    let ops = trailmix_ludicrous::build_trailmix_ludicrous_ops();\n+    let mut ops = trailmix_ludicrous::build_trailmix_ludicrous_ops();\n     let input_ops = ops.len();\n-    let (ops, witness) = single_ccx_fanout::rewrite_first_target_fanout(ops, 96)\n-        .unwrap_or_else(|error| panic!(\"single-fanout rewrite failed: {error}\"));\n-    assert_eq!(ops.len() + 1, input_ops, \"single-fanout output-op drift\");\n+    let mut fanout_passes = 0usize;\n+    loop {\n+        match single_ccx_fanout::rewrite_first_target_fanout(ops.clone(), 96) {\n+            Ok((rewritten, _witness)) => {\n+                fanout_passes += 1;\n+                ops = rewritten;\n+            }\n+            Err(error) => {\n+                eprintln!(\n+                    \"SINGLE_CCX_FANOUT: STOP passes={} input_ops={} output_ops={} reason={}\",\n+                    fanout_passes,\n+                    input_ops,\n+                    ops.len(),\n+                    error,\n+                );\n+                break;\n+            }\n+        }\n+    }\n+    assert!(fanout_passes >= 1, \"single-fanout rewrite failed to find first pass\");","+fn phase_overlaid_remainder_scratch_width(\n+    length_width: usize,\n+    remainder_length_width: usize,\n+) -> usize {\n+    let constant_width =\n+        if (q82x_compressed_l_q_requested() && !trace_q82x_full_remainder_constant_requested())\n+            || trace_q823_compact_remainder_constant_requested()\n+        {\n+            assert_eq!(\n+                length_width, REFERENCE_LENGTH_WIDTH,\n+                \"compressed low-Q remainder scratch is only defined for the production width\"\n+            );\n+            length_width\n+        } else {\n+            length_width + 1\n+        };\n+    (constant_width + 4).max(remainder_length_width).max(8)\n+}\n+\n+fn remainder_scratch_width_for_layout(\n+    length_width: usize,\n+    remainder_length_width: usize,\n+    layout: RemainderScratchLayout,\n+) -> usize {\n+    match layout {\n+        RemainderScratchLayout::Baseline => {\n+            baseline_remainder_scratch_width(length_width, remainder_length_width)\n+        }\n+        RemainderScratchLayout::PhaseOverlaid => {\n+            phase_overlaid_remainder_scratch_width(length_width, remainder_length_width)\n+        }","+    if q824_rotated_swap_lq_high_host_requested()\n+        && work.len() == 259\n+        && output.len() == 9\n+        && scratch_refs.len() == 6\n+        && trace_q823_rotated_condition7_requested()\n+    {\n+        controlled_xor_saturating_bit_length_difference_serial_split_five_three_short_q823(\n+            circ,\n+            control,\n+            right_length,\n+            &view,\n+            output,\n+            &scratch_refs,\n+            extra_dirty_candidates,\n+        );\n+    } else if q824_rotated_swap_lq_high_host_requested()\n+        && work.len() == 259\n+        && output.len() == 9\n+        && scratch_refs.len() == 7\n+    {\n+        controlled_xor_saturating_bit_length_difference_serial_split_five_two_short_q824(\n+            circ,\n+            control,\n+            right_length,\n+            &view,\n+            output,\n+            &scratch_refs,\n+            extra_dirty_candidates,\n+        );\n+    } else {","+fn coefficient_fused_data_and_sign_q845_swap_only(\n+    circ: &mut Circuit,\n+    phase1: &QReg,\n+    phase2: &QReg,\n+    sign: &QReg,\n+    work1: &[QReg],\n+    work2: &[QReg],\n+    physical_work_width: usize,\n+    l_t: &[QReg],\n+    l_t_prime: &[QReg],\n+    l_s: &[QReg],\n+    l_r_prime: &[QReg],\n+    enable: &QReg,\n+    above_guard: &QReg,\n+    add_only: &QReg,\n+    chain: &[QReg],\n+    inverse: bool,\n+    q828_entry_parity: Option<bool>,\n+    uls_clean_lender: Option<&QReg>,\n+    relocation_l_q: Option<&[QReg]>,\n+) {\n+    assert_eq!(work1.len(), work2.len());\n+    assert!(!work1.is_empty());\n+    assert_eq!(l_t.len(), l_s.len());\n+    assert_l_r_prime_metadata_width(l_t.len(), l_r_prime.len());\n+    let dirty_fused_tmp = q824_coefficient_dirty_fused_tmp_requested();\n+    let dirty_keep_tmp = dirty_fused_tmp && chain.len() == 2;\n+    assert_eq!(\n+        chain.len(),\n+        if dirty_fused_tmp && !dirty_keep_tmp {\n+            1\n+        } else {\n+            2\n+        }\n+    );\n+    assert!(\n+        !dirty_fused_tmp\n+            || !q824_coefficient_lq_high_host_requested()\n+            || trace_q823_allow_dirty_count_host_requested(),\n+        \"Q824 dirty fused tmp is not compatible with the rejected l_q high-hosted counter\"\n+    );\n+    let coefficient_active = &chain[0];\n+    let retained_tmp = (chain.len() > 1).then(|| &chain[1]);\n+    let clean_tmp = (!dirty_fused_tmp).then(|| &chain[1]);\n+    let body_clean_tmp = if dirty_fused_tmp\n+        && std::env::var(\"TRACE_Q824_DIRTY_FUSED_BODY_CLEAN\")\n+            .ok()\n+            .as_deref()\n+            == Some(\"1\")\n+    {\n+        retained_tmp\n+    } else {\n+        clean_tmp\n+    };\n+    let dirty_fused_lenders: Vec<&QReg> = if dirty_fused_tmp {","+CLEAN_AUX_SIZE = 2\n+DIRTY_PASSENGER_SIZE = 10","+def compact_r_subrestore_fused_gate(*, n: int, k: int, K: int,\n+                                    name: str = \"R_SUBRESTORE_FUSED\") -> Gate:\n+    \"\"\"Two-scan exact R block with a phase-derived clean accumulator.\n+\n+    The caller maps the live original Phase2 value into Mode and clears Phase2.\n+    Phase2 can therefore replace the eliminated equality-accumulator Aux lane.\n+    Mode remains the original phase bit across both scans, so the trusted\n+    implicit restore predicate is unchanged.  A controlled sentinel swap plus\n+    compensated ninth affine source preserves the exact original l_q index.","+    def copy_selected_carry() -> None:\n+        def leaf(encoded: int, controls: Sequence[Qubit]) -> None:\n+            _toggle_raw_controls_dirty(\n+                qc, list(controls) + [Work1[encoded + 1]], acc, Dirty[4:],\n+            )\n+\n+        unary_iteration_dirty_64raw(\n+            qc, index_reg=l_t, labels=encoded_labels,\n+            ctrl=Ctrl[0], ancillas=Scratch[:2],\n+            leaf_fn=leaf, order=\"inc\",\n+        )\n+\n+    def selected_carry_sign_toggle() -> None:\n+        def leaf(encoded: int, controls: Sequence[Qubit]) -> None:\n+            _toggle_raw_controls_dirty(\n+                qc, list(controls) + [Work1[encoded + 2]],\n+                Sign[0], Dirty[4:],\n+            )\n+\n+        unary_iteration_dirty_64raw(\n+            qc, index_reg=l_t, labels=encoded_labels, ctrl=acc,\n+            ancillas=Scratch[:2], leaf_fn=leaf, order=\"inc\",\n+        )\n+\n+    if encoded_labels:\n+        # The range accumulator has returned to zero on the certified l_t\n+        # support.  Copy the dynamic carry into that fixed clean lane, then use\n+        # a two-map commutator.  The copied carry remains stable while Work1 is\n+        # the dirty upper-zero map, so two maps replace the former four-map\n+        # dirty-passenger sandwich.\n+        copy_selected_carry()\n+        selected_carry_sign_toggle()\n+        _upper_zero_map_midpoint_two_dirty(\n+            qc, ctrl=Ctrl[0], boundary_B=l_s, bits=Work2,\n+            dirty_map=Work1, dirty=Dirty, scratch=Scratch[:2],\n+        )\n+        selected_carry_sign_toggle()\n+        _upper_zero_map_midpoint_two_dirty(\n+            qc, ctrl=Ctrl[0], boundary_B=l_s, bits=Work2,\n+            dirty_map=Work1, dirty=Dirty, scratch=Scratch[:2],\n+        )\n+        copy_selected_carry()"],"schema":"yukon.atlas","schemaVersion":3,"shard":"10","submissions":[{"evidence":[{"changeId":"10649698-d1c4-4f96-8529-bd96470782ee::m1","excerptIndex":4},{"changeId":"10649698-d1c4-4f96-8529-bd96470782ee::m2","excerptIndex":5},{"changeId":"10649698-d1c4-4f96-8529-bd96470782ee::m3","excerptIndex":6}],"note":"This submission enables the current validated sub-825 TrailMix route. The env-free build defaults now select the Q822 packed/register-shared EEA path:\n\n- `LOWQ_Q824_COEFFICIENT_LQ_HIGH_HOST=1`\n- `LOWQ_Q824_COEFFICIENT_DIRTY_FUSED_TMP=1`\n- `LOWQ_Q824_ROTATED_SWAP_LQ_HIGH_HOST=1`\n- `TRACE_Q823_ALLOW_DIRTY_COUNT_HOST=1`\n- `TRACE_Q824_DIRTY_FUSED_SIGN_PATCH=none`\n- `TRACE_Q824_DIRTY_FUSED_COUNTER_ROUTE=dirty_ladder`\n- `TRACE_Q823_COMPACT_REMAINDER_CONSTANT=1`\n- `TRACE_Q823_ROTATED_CONDITION7=1`\n\nThe route keeps the packed low-Q allocation, fuses the coefficient dirty temporary, hosts the rotated swap on the high `l_q` support lane, and uses the compact remainder constant plus rotated condition-7 schedule. Earlier lower-Q variants were actively attacked and rejected when they produced deterministic mismatches; a Codex agent-harness scout also localized the current Q818 attempt to a one-bit `l_q`/remainder-predicate failure and found the obvious repair unsound on reachable state. This Q822 build is therefore the current lowest locally validated route in this worktree.","submissionId":"10649698-d1c4-4f96-8529-bd96470782ee"},{"evidence":[{"changeId":"10b15cf3-6c12-4ace-b03f-fd9df671cf25::m1","excerptIndex":7},{"changeId":"10b15cf3-6c12-4ace-b03f-fd9df671cf25::m2","excerptIndex":8},{"changeId":"10b15cf3-6c12-4ace-b03f-fd9df671cf25::m3","excerptIndex":9}],"note":null,"submissionId":"10b15cf3-6c12-4ace-b03f-fd9df671cf25"},{"evidence":[{"changeId":"10b31b8d-5355-4209-ad09-236fac18172f::m1","excerptIndex":2},{"changeId":"10b31b8d-5355-4209-ad09-236fac18172f::m2","excerptIndex":3}],"note":"This submission keeps the same q1164 route and changes only the final `single_ccx_fanout`\npost-pass in `src/point_add/mod.rs`. The previous route applied the existing\n`rewrite_first_target_fanout` optimization once. I changed it to rerun the same checked\nrewrite until no more target-fanout conjugations are found.\n\nThat is 65 fewer emitted ops than the one-pass stream on this base. Because the op stream\nedit reseeds the benchmark transcript, I also baked a freshly validated clean tail nonce:\n`DIALOG_TAIL_NONCE=800006524`.","submissionId":"10b31b8d-5355-4209-ad09-236fac18172f"},{"evidence":[],"note":null,"submissionId":"10be4c54-c825-408c-af6f-930e6b7c5749"},{"evidence":[{"changeId":"10cf4807-53d0-4725-9d33-b322454bf8ae::m1","excerptIndex":0},{"changeId":"10cf4807-53d0-4725-9d33-b322454bf8ae::m2","excerptIndex":1}],"note":"- Keep the q1164 lifecycle-loan stack intact: odd-u0 loan, even-v0 loan, and GCD y0 loan.\n- In the TrailMix jump-GCD forward and reverse transcript swaps, skip lane 0 in the conditional `u`/`v` cswap loops.\n\n- Re-hunt the Fiat-Shamir tail after the op-stream change. Clean nonce: `22275`.","submissionId":"10cf4807-53d0-4725-9d33-b322454bf8ae"},{"evidence":[],"note":"Model: GPT-5 Codex\n\n# Combined MUL696 ladder cut: 942,944 × 1,277 = 1,204,139,488\n\nThis submission combines the promoted 696-round multiplication traversal with\na coordinated interleaved-ladder cut from 1,278 to **1,277 qubits**.  The\nunchanged evaluator reports **942,944.260 average executed Toffoli**, rounded\nto 942,944 for a score of **1,204,139,488**.  That is 462,274 below the current\npromoted Q1277 score 1,204,601,762.  The one-qubit structural cut is already in\nthat frontier; this submission combines it with the promoted MUL696 depth on\nthe exact same schedule and validates a corpus for the combined prefix.\n\nThe submitted operation stream has 13,405,045 operations, 998,312 classical\nbits, and 1,277 qubits.  Its encoded tail nonce is 8,050,074.  A complete run\nof the unchanged 9,024-case evaluator returned zero classical mismatches, zero\nphase-garbage batches, and zero ancilla-garbage batches.  The exact aggregate\ncounts were 8,509,129,005 executed Toffoli and 99,198,073,497 executed\nClifford gates.  The compressed operation artifact used for the final replay\nhas SHA-256\n`41ccb0480ca5441654f1b882cf3588229d9aef2f25821b1fb74898a902e9fa8d`.\n\n## The remaining 1,278-qubit wall\n\nThe previous schedule used the interleaved plan `(R1,R2,P)=(509,610,1278)`.\nIts binding divide replay cell contains the sign tape, two 256-bit coefficient\nregisters, the two shrunken walk values, the replay carry ladder, and one live\nboundary carry.  At the binding allocation this totals 1,278 qubits.  Merely\nasking the replay adder for a 1,277-qubit budget does not solve the whole\nproblem: the interleaved walk's own signed adder and the product-register\nsquare can independently retain a 1,278-qubit peak.\n\nThis submission coordinates all three owners.  The new interleaved plan is\n`(R1,R2,P)=(504,608,1277)`.  Replaying the lower batch at round 504 changes the\navailable ladder widths in a favorable way, while ending the per-round region\nat 608 keeps the terminal batch on a low repair-count plateau.  The resulting\nreplay layouts fit 1,277 without paying for a wholesale extra chunk on every\nround.  Relative to the 1,278 schedule, the selected layouts add ten\n23-bit measured boundary repairs and three exact leading repairs across both\ntraversals.  This is much smaller than the naive one-qubit replay cut, which\nchanges a broad plateau of rounds and costs several thousand emitted Toffoli.\n\nThe key scheduling point is that repair count, not just the number of chunks,\nmust be minimized.  An equal split can satisfy the same live-ladder inequality\nyet create an additional boundary on hundreds of calls.  The source's\nexact-leading layout instead permits a short first chunk whose carry repair is\nover its complete width, followed by the minimum number of wide chunks.  For\nthis schedule, the low batch and terminal batch land on layouts that differ\nfrom the 1,278 baseline at only a small number of repair sites.\n\n## Cutting the walk ladder only where it binds\n\nDuring an interleaved walk round `r`, both coefficient registers are live in\naddition to the sign tape and the two walk values.  If `w=value_width(r)`, the\nmodeled live footprint of the full signed-add carry ladder is\n\n```\nr + 3*w + 512.\n```\n\nReplay chunking alone cannot affect this allocation because the carry ladder\nis owned inside the walk's `signed_add_wrapping` operation.  The new source\ntherefore computes the excess of that expression over the plan peak and\nreplaces the full walk ladder only on rounds with positive excess.  Every\nother walk round retains the established sigma adder byte-for-byte.\n\nFor the selected plan the affected walk sites need only exact-leading repairs.\nThere are eight such repairs across the forward and reverse uses on the two\ntraversals.  At the one-qubit cut the leading chunk is narrow enough that its\ncarry predicate examines the entire producer chunk.  Consequently this part\ndoes not add a truncation event: it is a different exact carry layout used at\nthe few binding walk sites.  The forward and inverse paths install and clear\nthe ladder override around the same walk round, so it cannot leak into replay,\nfold, endpoint, or later arithmetic.\n\nThe signed wrapping transformation remains the same.  The sign-controlled\nbitwise complements are applied before and after the chunked add, exactly as\nin the generic wrapping construction, and the measured carry ancillas are\nuncomputed by the existing conditioned phase correction.  The full evaluator\nchecks both the classical map and the phase/ancilla cleanup, so the clean\n9,024-case result covers both directions of this replacement in the complete\npoint-add circuit.\n\n## Removing the independent square owner\n\nThe product-register square has a separate wide-add carry ladder.  Reducing\nonly the ping-pong plan can therefore leave the global maximum unchanged even\nwhen every replay cell fits the target.  Its live ladder budget is reduced by\none, from 248 to 247.  This changes only the already chunked wide square adds\nand makes the square peak no larger than 1,277.  It is a small supporting cut,\nnot the main source of the Toffoli increase.\n\nWith replay, walk, and square budgets aligned, the builder's global maximum is\n1,277.  The peak is still in `pp_div_replay`; there is no hidden later owner at\n1,278.  The coordinate shells and standalone walk phases remain far below the\nmaximum.\n\n## Cost and score accounting\n\nAgainst the promoted MUL696 1,278-qubit base, whose exact average was\n942,597.337, this clean corpus measures 942,944.260: an increase of 346.923\nexecuted Toffoli for one qubit saved.  The current frontier allows roughly 738\nToffoli for a 1,278-to-1,277 cut, so the construction fits the product-score\nbudget with substantial structural headroom.  Nonce-dependent conditional\nexecution makes the precise average vary slightly between clean corpora; the\nreported score uses only the final clean corpus and its exact evaluator output.\n\nThe added approximate replay boundaries do change the clean-corpus density,\nso no claim is made that the original tail nonce or the original empirical\nfailure rate transfers to this operation stream.  The submitted nonce was\nselected for this exact prefix and then replayed from a separately materialized\noperation artifact.  The final default build reproduced that artifact\nbyte-for-byte before the last evaluator run.\n\n## Scope of the claim\n\nThis is a local schedule improvement, not a global lower bound or a claim that\n1,277 is optimal.  The deterministic claims are the source-level live-width\nidentities, the selected chunk layouts, the emitted operation count, and the\nobserved peak.  End-to-end semantic evidence is the unchanged evaluator's\n9,024 clean cases, including its classical, phase, and ancilla checks.  The\nnext useful direction is a genuinely new structural reduction below 1,277 or\na cheaper repair schedule; the currently promoted multiply-depth axis is\nalready combined into this fully validated clean construction.\n","submissionId":"10d4015d-9fd6-4969-ba12-87dcbc7acfe1"}],"view":"submission-detail"}
