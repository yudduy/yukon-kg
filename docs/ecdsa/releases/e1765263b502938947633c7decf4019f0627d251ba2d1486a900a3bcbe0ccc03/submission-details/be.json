{"excerpts":["-    set_default_env(\"DIALOG_GCD_APPLY_CHUNKED_F_CUT\", \"70\");\n+    // F_CUT=82 (not 70): rebalances the chunked apply add/sub split so the big\n+    // chunk shrinks from [70,257)=187 to [82,257)=175 wide. Peak = base +\n+    // 2·(big-chunk width), so this drops the apply peak 1558→1542 and the global\n+    // peak 1558→1543 (now bound by round84 x-tail square). Cost +9,576 executed\n+    // Toffoli (exact boundary-clear comparator 70→82 bits × 399 steps × 2). Net\n+    // −10.5M score. Co-tuned reroll 9/4. (F_CUT=78 would be cheaper but has no\n+    // clean FS island in the standard grid; 82 is the lowest-cost peak-1543 cut\n+    // that lands clean.)\n+    set_default_env(\"DIALOG_GCD_APPLY_CHUNKED_F_CUT\", \"82\");","     // Compare58 requires a fresh Fiat-Shamir island: 1/14 validates 0/0/0 over\n     // 9024 at 1567q and 1,682,963 avg executed Toffoli.\n-    set_default_env(\"DIALOG_REROLL\", \"13\");\n-    set_default_env(\"DIALOG_POST_SUB_REROLL\", \"14\");\n+    set_default_env(\"DIALOG_REROLL\", \"9\");\n+    set_default_env(\"DIALOG_POST_SUB_REROLL\", \"4\");","+pub fn build() -> Vec<Op> {\n+    if std::env::var(\"POINT_ADD_DIALOG_ROUTE\").ok().as_deref() != Some(\"1\") {\n+        return trailmix_port::build_builder().ops;\n+    }\n+    if std::env::var(\"DIALOG_GCD_K5_HEAD11_SELFTEST\").is_ok() {","+/// Unconditional `a := a + b mod 2^n`, specialized from\n+/// [`controlled_add_cuccaro_3n_refs`] with its control fixed to `|1>` and using\n+/// a caller-owned clean carry lane.\n+///\n+/// `carry` must enter in `|0>`, must not alias either operand, and is restored\n+/// to `|0>` by the reverse ripple. The forward carry ripple and its reverse are\n+/// gate-for-gate identical to the fixed-control specialization. Only\n+/// `CCX(ctrl, b[i], a[i])` is reduced to `CX(b[i], a[i])`.\n+pub fn add_cuccaro_3n_uncontrolled_refs_with_carry(\n+    circ: &mut Circuit,\n+    a: &[&QReg],\n+    b: &[&QReg],\n+    carry: &QReg,\n+) {\n+    let n = a.len();\n+    assert_eq!(\n+        b.len(),\n+        n,\n+        \"add_cuccaro_3n_uncontrolled_refs: a/b length mismatch\"\n+    );\n+    assert!(\n+        !a.iter().chain(b.iter()).any(|q| q.id() == carry.id()),\n+        \"add_cuccaro_3n_uncontrolled_refs_with_carry: carry aliases an operand\"\n+    );\n+\n+    if n == 0 {\n+        return;\n+    }\n+    if n == 1 {\n+        circ.cx(b[0], a[0]);\n+        return;\n+    }\n+\n+    // This is the controlled primitive's forward MAJ chain unchanged.\n+    for i in 0..n {\n+        circ.cx(carry, b[i]);\n+        circ.cx(carry, a[i]);\n+        circ.ccx(a[i], b[i], carry);\n+    }\n+\n+    // Fixing ctrl=1 turns only the sum write into a CNOT. The surrounding\n+    // carry uncompute remains literal, so c and b return to their inputs.\n+    for i in (0..n).rev() {\n+        circ.ccx(a[i], b[i], carry);\n+        circ.cx(carry, a[i]);\n+        circ.cx(b[i], a[i]);\n+        circ.cx(carry, b[i]);\n+    }\n+}\n+\n+/// Fixed-control Cuccaro specialization with an internally allocated carry.\n+pub fn add_cuccaro_3n_uncontrolled_refs(circ: &mut Circuit, a: &[&QReg], b: &[&QReg]) {\n+    if a.len() <= 1 {\n+        assert_eq!(\n+            a.len(),\n+            b.len(),\n+            \"add_cuccaro_3n_uncontrolled_refs: a/b length mismatch\"\n+        );\n+        if a.len() == 1 {\n+            circ.cx(b[0], a[0]);\n+        }\n+        return;\n+    }\n+\n+    let c = circ.alloc_qreg(\"ccuccaro3n_c\");\n+    add_cuccaro_3n_uncontrolled_refs_with_carry(circ, a, b, &c);\n+    circ.zero_and_free(c);\n+}","+fn lowq_borrow_passenger_carry_enabled() -> bool {\n+    if std::env::var(\"LOWQ_BORROW_PASSENGER_CARRY\")\n+        .ok()\n+        .as_deref()\n+        != Some(\"1\")\n+    {\n+        return false;\n+    }\n+    assert!(\n+        lowq_one_a_elim_enabled(),\n+        \"LOWQ_BORROW_PASSENGER_CARRY requires LOWQ_ONE_A_ELIM=1\"\n+    );\n+    assert_eq!(\n+        std::env::var(\"LOWQ_CLZ_DIFF_CONST_FOLD\").ok().as_deref(),\n+        Some(\"1\"),\n+        \"LOWQ_BORROW_PASSENGER_CARRY requires LOWQ_CLZ_DIFF_CONST_FOLD=1\"\n+    );\n+    true","+fn trailmix_q_width_step(wq: usize, wa: usize, wb: usize, wca: usize, wcb: usize) -> usize {\n+    let natural = wq.max(1);\n+    let target = std::env::var(\"TRAILMIX_Q_TARGET\")\n+        .ok()\n+        .and_then(|s| s.parse::<usize>().ok());\n+    let Some(target) = target else {\n+        return trailmix_q_width(wq);\n+    };\n+    // q budget is computed from the (possibly capped) A/B and ca/cb widths so the\n+    // working width 2*ab + 2*cacb + q meets `target` consistently with the resizes.\n+    let other = 2 * trailmix_ab_width(wa.max(wb)) + 2 * trailmix_cacb_width(wca.max(wcb));\n+    let budget = target.saturating_sub(other).max(1);\n+    // Still honor a global Q_CAP if both are set (take the tighter bound).\n+    let capped = natural.min(budget);\n+    std::env::var(\"TRAILMIX_Q_CAP\")\n+        .ok()\n+        .and_then(|s| s.parse::<usize>().ok())\n+        .map_or(capped, |cap| capped.min(cap.max(1)))\n+        .max(1)","+    let const_fold = lowq_clz_diff_const_fold_enabled();\n+    if const_fold {\n+        // Constants commute across the subtract. This is the q980 reduction:\n+        // one modular constant add instead of two, with no extra live wires.\n+        {\n+            let par: Vec<&QReg> = pa.iter().collect();\n+            let pbr: Vec<&QReg> = pb.iter().collect();\n+            sub_refs(circ, &par, &pbr);\n+        }\n+        add_pa(circ, &pa, lo_a as i64 - lo_b as i64);\n+    } else {\n+        {\n+            let par: Vec<&QReg> = pa.iter().collect();\n+            let pbr: Vec<&QReg> = pb.iter().collect();\n+            add_pa(circ, &pa, 1 + lo_a as i64);\n+            sub_refs(circ, &par, &pbr);\n+        }\n+        add_pa(circ, &pa, -(1 + lo_b as i64));\n+    }\n+\n+    body(circ, &pa); // USE pa (= diff)\n+\n+    if const_fold {\n+        {\n+            let par: Vec<&QReg> = pa.iter().collect();\n+            let pbr: Vec<&QReg> = pb.iter().collect();\n+            add_refs(circ, &par, &pbr, borrowed_carry);\n+        }\n+        add_pa(circ, &pa, lo_b as i64 - lo_a as i64);","+    if let Some(nonce) = std::env::var(\"TRAILMIX_TAIL_NONCE\")\n+        .ok()\n+        .and_then(|s| s.parse::<u64>().ok())\n+    {\n+        circ.set_section(\"trailmix_tail_nonce\");\n+        for i in 0..TRAILMIX_TAIL_NONCE_BITS {\n+            let q = if (nonce >> i) & 1 == 1 {\n+                &out[1]\n+            } else {\n+                &out[0]\n+            };\n+            circ.x(q);\n+            circ.x(q);\n+        }","+        // 4) cswap(swap_flag, u, v). For i>=1, bit 0 is skipped: the odd-u\n+        //    invariant gives u[0]=1, and subtracted=v[0], so when swap_flag=1\n+        //    both are 1 (no-op swap); when swap_flag=0 no swap anyway.\n+        let j_start = if i > 0 { 1 } else { 0 };\n+        for j in j_start..current_n {\n+            circ.cswap(swap_flag, u[j], v[j]);\n+        }","+    circ.cx(*ctrl, y[0]); // bit-0 sum (x[0] == 1 under the odd-u invariant)\n     if x.len() > 1 {\n         let yr: Vec<&QubitId> = y[1..].iter().collect();\n         let xr: Vec<&QubitId> = x[1..].iter().collect();\n         super::gidney::controlled_hybrid_add_capped_branch(circ, ctrl, &yr, &xr, k, super::PAD, branch);","+    // Stage 1: prod = lambda^2 (integer, 2n bits).\n+    let mut prod: Vec<QubitId> = Vec::with_capacity(2 * n);\n+    symmetric_square_into_prod(circ, lambda, &mut prod);\n+\n+    // Stage 2: output -= (lo + f*hi) mod q, operating on prod's own halves.\n+    //   lo = prod[0..n]                                  (n-bit, lo can be >= q)\n+    //   hi = prod[n..2n]                                 (n-bit, hi < q)\n+    // mod_double needs a 257-bit operand whose top bit is |0>; one inserted pad\n+    // above hi supplies that overflow slot (restored, removed at the end).\n+    {\n+\n+        // --- lo term: output -= lo mod q ---\n+        // lo = prod[0..n] is a full integer < 2^256 (not pre-reduced), but\n+        // mod_sub subtracts mod q, which is the value we want:\n+        // lambda^2 mod q == (lo + f*hi) mod q == (lo mod q + ...).\n+        // UNCONTROLLED: no |1>-gated register-sub CCX.\n+        mod_sub(circ, &prod[0..n], output_reg);\n+\n+        let pad_hi = circ.alloc_qubit();\n+        let mut hi_ext: Vec<QubitId> = prod[n..2 * n].to_vec();\n+        hi_ext.push(pad_hi); // index 2n = overflow slot, |0>\n+\n+        // --- hi terms: output -= (hi*2^j mod q) for each f-bit j ---\n+        let mut shifted = 0usize;\n+        for &j in &F_BITS {\n+            while shifted < j {\n+                mod_double(circ, &hi_ext); // hi *= 2 mod q (257-bit, pad restored)\n+                shifted += 1;\n+            }\n+            mod_sub(circ, &hi_ext[0..n], output_reg);\n+        }\n+        // restore hi to its raw value (matched-reverse the doublings). mod_double\n+        // is built from a swap chain (self-inverse) + a +f window (NOT self-\n+        // inverse), so a literal re-run does NOT undo it -- use the value-inverse\n+        // mod_double_reverse (one halving per forward doubling).\n+        while shifted > 0 {\n+            mod_double_reverse(circ, &hi_ext);\n+            shifted -= 1;\n+        }","+// Auto-generated Task 7 stable-ancestry deep strip keys.\n+// Confirmed over 100,000,000 training inputs plus disjoint 10,000,000-input holdout.\n+// Final exact intersection: 6,633 DEAD_KEYS and 1,255 DOWNGRADE_KEYS.\n+// Generated from artifacts/task7/confirmed-intersection.rs; do not edit by hand.\n+\n pub static DEAD_KEYS: &[(u8, u64, u64, u64, u64, u32, u32)] = &[\n-    (13, 54, 822, 1079, 18446744073709551615, 0, 1),","+pub(crate) fn run(ops: &[Op]) {\n+    let mode = std::env::var(\"TLM_GRIND_MODE\").unwrap_or_else(|_| \"lambda\".to_string());\n+    let nonces = env_u64(\"TLM_GRIND_NONCES\", 16);\n+    let start = env_u64(\"TLM_GRIND_START\", 0);\n+    let threads = env_usize(\n+        \"TLM_GRIND_THREADS\",\n+        std::thread::available_parallelism().map(|n| n.get()).unwrap_or(4),\n+    );\n+    let max_batches = env_usize(\"TLM_GRIND_BATCHES\", usize::MAX);\n+\n+    let (num_q, num_b, _nregs, regs) = analyze_ops(ops.iter());\n+    let (num_q, num_b) = (num_q as usize, num_b as usize);\n+\n+    eprintln!(\n+        \"GRIND start: mode={mode} ops={} qubits={num_q} bits={num_b} nonces={nonces} start={start} threads={threads}\",\n+        ops.len()","+    set_default_env(\"LUD_EXTRA_FOLD_VENTS\", \"0\");\n+    set_default_env(\"LUD_EXTRA_FOLD_MIN_G\", \"0\");\n+    set_default_env(\"LUD_EXTRA_FOLD_MAX_G\", \"999\");\n+    set_default_env(\"DIALOG_TAIL_NONCE\", \"2430844\");\n+    set_default_env(\"TLM_FOLD_TAIL_CINC\", \"1\");\n+    set_default_env(\"TLM_CODEC_DIAMOND_MCX\", \"1\");\n+    set_default_env(\"SINGLE_CCX_FANOUT_DISABLE\", \"0\");\n+\n+    set_default_env(\"TLM_SQUARE_F_RAMP10_DIRECT32_TAGS\", \"\");\n+    set_default_env(\"TLM_SQUARE_F_SHIFTED_LOW\", \"1\");\n+\n+    set_default_env(\"TLM_GRAD_FINAL_NO_COUT\", \"1\");\n+    set_default_env(\"TLM_APPLY_FWD_FIRST_CSWAP_SKIP\", \"1\");\n+    set_default_env(\"CONSTPROP_MAX_ITERS\", \"16\");\n+\n+    set_default_env(\"TLM_TARGET_Q\", \"1155\");\n+    set_default_env(\"TLM_FOLD_BOUNDARY_ZERO_DIRECT\", \"1\");\n+    set_default_env(\"TLM_FOLD_CHUNK_FORCE\", \"4\");\n+    set_default_env(\"TLM_TARGET_FOLD_CALL_RESERVE_OVERRIDES\", \"173:3,175:3,177:3,256:11,257:11,336:3,338:3,340:3,176:3,178:3,180:3,254:5,259:20,333:3,335:3,337:3,179:3,181:3,183:3,182:3,184:3,186:3,327:3,329:3,330:3,331:3,332:3,334:3\");\n+    set_default_env(\"TLM_TARGET_FFG_CALL_RESERVE_OVERRIDES\", \"184:4,186:4,188:4,205:6,207:6,209:6,220:7,222:7,224:7,238:8,240:8,242:8,251:9,257:10,262:10,355:10,362:10,359:10,181:3,183:3,185:3,187:4,189:4,191:4,196:5,198:5,200:5,208:6,210:6,212:6,223:7,225:7,227:7,241:8,243:8,245:8,250:9,252:9,190:4,192:4,193:5,194:4,195:5,197:5,199:5,201:5,202:6,203:5,204:6,206:6,211:6,213:6,214:7,215:6,216:7,218:7,226:7,228:8,229:8,230:8,231:8,233:8,244:8,246:8,247:9,253:9,254:10,259:11,358:10,340:11,341:11,342:11,343:11,344:11,345:11,346:11,347:11,348:11,349:11,350:11\");\n+    set_default_env(\"TLM_APPLY_FWD_S2_ZERO_LAST\", \"1\");\n+    set_default_env(\"TLM_APPLY_INV_S2_ZERO_LAST\", \"1\");\n+    set_default_env(\"TLM_APPLY_FWD_CSWAP_SKIP_LAST\", \"2\");\n+    set_default_env(\"TLM_APPLY_INV_CSWAP_SKIP_LAST\", \"1\");\n+    set_default_env(\"TLM_FOLD_RELEASE_CONTROLS\", \"1\");","+    let ccz_bypassed = std::env::var(\"TLM_CCZ_SELF_INVERSE_CANCEL\").ok().as_deref() == Some(\"0\");\n+    let ccz_mask = if op_site_diag && !ccz_bypassed {\n+        Some(trailmix_ludicrous::constprop::ccz_straddle_kill_mask_pub(&ops))\n+    } else {\n+        None\n+    };\n+    let ops = ccz_self_inverse_cancel(ops);\n+    if op_site_diag {\n+        if let Some(killed) = ccz_mask {\n+            let kept: Vec<bool> = killed.iter().map(|&k| !k).collect();\n+            sites = deep_strip_sites::filter_sites_by_kept(&sites, &kept);\n+        }\n+        assert_eq!(sites.len(), ops.len(), \"OpSite trace misaligned after ccz_self_inverse_cancel\");\n+    }\n+\n+    let ccx_final_mask =\n+        if op_site_diag { trailmix_ludicrous::constprop::ccx_final_cancel_kill_mask(&ops) } else { None };\n+    let mut ops = trailmix_ludicrous::constprop::ccx_final_cancel(ops);\n+    if op_site_diag {\n+        if let Some(killed) = ccx_final_mask {\n+            let kept: Vec<bool> = killed.iter().map(|&k| !k).collect();\n+            sites = deep_strip_sites::filter_sites_by_kept(&sites, &kept);\n+        }\n+        assert_eq!(sites.len(), ops.len(), \"OpSite trace misaligned after ccx_final_cancel (cancellation checkpoint)\");\n+    }","-pub fn controlled_mod_add_k(circ: &mut B, ctrl: &QubitId, x: &[QubitId], y: &[QubitId], sched_k: Option<usize>, ffg_g: Option<usize>) {\n+pub fn controlled_mod_add_k(circ: &mut B, ctrl: &QubitId, x: &[QubitId], y: &[QubitId], sched_k: Option<usize>, ffg_g: Option<usize>, lsbs: usize) {\n     let n = x.len();\n     assert_eq!(y.len(), n, \"x,y must both be n=256 bits\");\n     assert_eq!(n, 256, \"secp256k1 controlled_mod_add expects n=256\");\n+    assert!(F_BITLEN <= lsbs && lsbs < 128, \"controlled_mod_add_k: lsbs {lsbs} out of range\");\n     let f_bytes = F_SECP256K1.to_le_bytes();\n     let anc = circ.alloc_qubit();\n \n@@ -1528,7 +1529,7 @@ pub fn controlled_mod_add_k(circ: &mut B, ctrl: &QubitId, x: &[QubitId], y: &[Qu\n     }\n \n     circ.set_phase(\"tlm_apply_forward_mod_add_fold\");\n-    add_f_window(circ, &anc, y, LSBS, &f_bytes, ffg_g);\n+    add_f_window(circ, &anc, y, lsbs, &f_bytes, ffg_g);","+pub const JUMP: usize = 2;\n+\n+pub const ITERS: usize = 259;\n+\n+/// The divstep count that every baked artefact in this crate was fitted\n+/// against: the eight schedule vectors below, and every census-mined dead-gate\n+/// certificate (those are keyed by bare per-helper call counters or by absolute\n+/// op ordinals). `ITERS` may move; this constant may not. When the two differ\n+/// the schedule vectors are re-blocked (`widen_sched_blocks`) and the\n+/// certificates are retired (`apply_drops_off`, and the deep strip in `build`).\n+pub const BAKED_ITERS: usize = 258;\n+\n+/// The circuit geometry revision this source tree currently builds. Bump this whenever\n+/// an edit changes op ordinals or per-helper call counts along a path the 18 baked\n+/// drop-certificate families are keyed against -- even an edit that leaves `ITERS`\n+/// itself unchanged (e.g. the coupled schedule-margin / contextual-LSBS plumbing).\n+pub const CURRENT_GEOMETRY_REV: u32 = 2;\n+\n+/// The geometry revision the 18 baked drop-certificate families (`drops_off_family`)\n+/// were last independently re-certified against. `ITERS == BAKED_ITERS` alone is not\n+/// sufficient to trust them: it is possible to land back on the same iteration count\n+/// (as this crate does going from `ITERS = 261` to `ITERS = 258 == BAKED_ITERS`) while\n+/// the geometry those keys were mined against has moved out from under them in the\n+/// interim. Bump this to match `CURRENT_GEOMETRY_REV` only once the 18 families have\n+/// been independently re-mined and validated against the new geometry.\n+pub const DROP_CERT_GEOMETRY_REV: u32 = 1;\n+\n+/// Geometry-identity check the 18 baked drop-certificate families must pass before\n+/// `drops_off_family` will trust them. Pure and explicit (rather than folded into\n+/// `baked_artifacts_valid`'s no-argument form) so both the iteration count and the\n+/// geometry revision are independently testable without needing to mutate the `const`\n+/// `ITERS`.\n+#[must_use]\n+pub const fn baked_artifacts_valid_for(iters: usize, geometry_rev: u32) -> bool {\n+    iters == BAKED_ITERS && geometry_rev == DROP_CERT_GEOMETRY_REV\n+}"],"schema":"yukon.atlas","schemaVersion":3,"shard":"be","submissions":[{"evidence":[],"note":"Tighten dialog-GCD compare window from 63 to 60 and co-tune free Fiat-Shamir reroll to 9.","submissionId":"be7ace2c-3b6c-4199-9e34-fbe8fc5c03c4"},{"evidence":[],"note":"# Branch-comparator width 61→59 on the chunked-apply + round763 base","submissionId":"beccfc9a-8db8-4569-a6bb-8cbaedfb3243"},{"evidence":[{"changeId":"bef8d8da-46a4-4ff8-84d1-0e81bfc5d4a4::m1","excerptIndex":0},{"changeId":"bef8d8da-46a4-4ff8-84d1-0e81bfc5d4a4::m2","excerptIndex":1}],"note":"# Apply chunked add/sub: rebalance F_CUT 70→82 (peak 1558 → 1543)\n\nThe op-stream shift re-rolls the Fiat-Shamir island; co-tuned to `DIALOG_REROLL=9`,\n`DIALOG_POST_SUB_REROLL=4` (clean 0/0/0). (F_CUT=78 reaches peak 1543 at only +6,384","submissionId":"bef8d8da-46a4-4ff8-84d1-0e81bfc5d4a4"},{"evidence":[],"note":null,"submissionId":"be808e33-eafb-4ecd-8adb-3c50567bb343"},{"evidence":[{"changeId":"be22a52e-4493-427e-aa83-a7631f28e1a5::m1","excerptIndex":2},{"changeId":"be22a52e-4493-427e-aa83-a7631f28e1a5::m2","excerptIndex":3},{"changeId":"be22a52e-4493-427e-aa83-a7631f28e1a5::m3","excerptIndex":4},{"changeId":"be22a52e-4493-427e-aa83-a7631f28e1a5::m4","excerptIndex":5},{"changeId":"be22a52e-4493-427e-aa83-a7631f28e1a5::m5","excerptIndex":6},{"changeId":"be22a52e-4493-427e-aa83-a7631f28e1a5::m6","excerptIndex":7}],"note":null,"submissionId":"be22a52e-4493-427e-aa83-a7631f28e1a5"},{"evidence":[{"changeId":"befd8918-7565-4f87-90ce-d6eee6f39719::m1","excerptIndex":8},{"changeId":"befd8918-7565-4f87-90ce-d6eee6f39719::m2","excerptIndex":9},{"changeId":"befd8918-7565-4f87-90ce-d6eee6f39719::m3","excerptIndex":10}],"note":"Skip bit-0 of GCD cswap for i>0 (odd-u invariant: u[0]=v[0]=1 when swap fires). -1,023 avg Toffoli. Nonce 13003.","submissionId":"befd8918-7565-4f87-90ce-d6eee6f39719"},{"evidence":[{"changeId":"be6290ba-4541-434a-95da-8544a0e6acf7::m1","excerptIndex":11},{"changeId":"be6290ba-4541-434a-95da-8544a0e6acf7::m2","excerptIndex":12},{"changeId":"be6290ba-4541-434a-95da-8544a0e6acf7::m3","excerptIndex":13},{"changeId":"be6290ba-4541-434a-95da-8544a0e6acf7::m4","excerptIndex":14},{"changeId":"be6290ba-4541-434a-95da-8544a0e6acf7::m5","excerptIndex":15},{"changeId":"be6290ba-4541-434a-95da-8544a0e6acf7::m6","excerptIndex":16}],"note":null,"submissionId":"be6290ba-4541-434a-95da-8544a0e6acf7"}],"view":"submission-detail"}
