{"excerpts":["+    set_default_env(\"TLM_SQUARE_F_RAMP10_DIRECT32_TAGS\", \"\");\n+    set_default_env(\"TLM_SQUARE_F_SHIFTED_LOW\", \"1\");","-    let scheduled_g = g_sched\n-        .map_or_else(|| CEILING.saturating_sub(circ.active_qubits as usize), |g| g)\n-        .min(target_g.unwrap_or(usize::MAX))\n-        .min(n - 1);\n-    let g = std::env::var(\"TLM_FFG_FORCE_G\")\n-        .ok()\n-        .and_then(|value| value.parse::<usize>().ok())\n-        .map_or(scheduled_g, |forced| forced.min(n - 1));\n-    let trace_entry_active = circ.active_qubits;\n+    let g = g_sched.map_or_else(|| CEILING.saturating_sub(circ.active_qubits as usize), |g| g).min(n - 1);","-pub fn jump_dialog_regions(n3: usize, iters: usize) -> Vec<(DialogCodec, usize)> {\n+pub fn jump_dialog_regions(n3: usize, iters: usize) -> Vec<(DialogCodec, usize)> {\n     // Symbol 0 is the 1-symbol Step0 codec. The remaining iters-1 symbols tile.\n-    let tail4 = usize::from(tail4_top32_enabled() && iters >= 6) * 5;\n-    let codec_syms = iters - 1 - tail4;\n+    let codec_syms = iters - 1;","+    // Held-carry count for the chunked comparator, supplied per call by the\n+    // schedule (`ck` Gidney carries held, `ck+1` live).\n+    let ck = super::next_cmp_k();\n+    let lt_flag = circ.alloc_qubit();\n+    compare_geq_chunked_middle(\n+        circ,\n+        &u_top,\n+        &v_top,\n+        &lt_flag,\n+        |c, flag| {\n+            c.x(*flag);\n+            c.ccx(*ctrl, *flag, *target);\n+            c.x(*flag);\n+        },\n+        ck,\n+    );\n+    circ.zero_and_free(lt_flag);","+                // Target becomes a fresh (nonlinear) variable. If we proved the\n+                // controls equal, the gate behaves as CX(a,t) -> t ^= a; but a\n+                // fresh var is a sound over-approximation of the resulting value\n+                // and keeps the analysis simple/exact for relations.\n+                af.set[t] = af.fresh();\n+                af.cst[t] = false;","+fn coord_addsub(circ: &mut B, dst: &[QubitId], coord: &[BitId], subtract: bool) {\n+    debug_assert_eq!(dst.len(), N);\n+    debug_assert_eq!(coord.len(), N);\n+    let temp = circ.alloc_qubits(N);\n     for i in 0..N {\n         circ.x_if_bit(temp[i], coord[i]); // load: temp := coord (per-shot classical)\n     }"],"schema":"yukon.atlas","schemaVersion":3,"shard":"fe","submissions":[{"evidence":[{"changeId":"fe4999b2-cba6-4025-afaf-6e9293e238d4::m1","excerptIndex":0},{"changeId":"fe4999b2-cba6-4025-afaf-6e9293e238d4::m2","excerptIndex":1},{"changeId":"fe4999b2-cba6-4025-afaf-6e9293e238d4::m3","excerptIndex":2},{"changeId":"fe4999b2-cba6-4025-afaf-6e9293e238d4::m4","excerptIndex":3},{"changeId":"fe4999b2-cba6-4025-afaf-6e9293e238d4::m5","excerptIndex":4},{"changeId":"fe4999b2-cba6-4025-afaf-6e9293e238d4::m6","excerptIndex":5}],"note":null,"submissionId":"fe4999b2-cba6-4025-afaf-6e9293e238d4"}],"view":"submission-detail"}
